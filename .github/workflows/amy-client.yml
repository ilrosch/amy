name: amy-client

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4

      - name: dependency installation
        run: npm ci --omit=dev

      - name: running linter
        run: npm run lint